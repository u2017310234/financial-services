# Sample data

This skill does not bundle any third-party datasets.

Provide your own inputs via:
- `local_path` (CSV/XLSX) for `data.*` and some `options.*` / `portfolio.*` tasks, or
- inline `records` (`{"index": ..., "columns": ..., "data": ...}`) in the task payload.

